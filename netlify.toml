# Configuración de despliegue en Netlify para ShowClinic (Vite + React)
[build]
  command = "npm run build"
  publish = "dist"

# Redirección para apps de una sola página (SPA): cualquier ruta carga index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
